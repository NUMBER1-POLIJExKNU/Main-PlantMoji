# 게임 체험모드 (Trial Mode) 구현 기획안

> 인도네시아 고등학생 시연용 — 치트모드 앞단에 놓이는 "짧고 진한" 온보딩 게임 모드.

---

## 1. 배경과 목적

- 대상: 이 앱에 흥미가 없다고 가정한 인도네시아 고등학생. 첫인상 2분 안에 게임 루프(관찰 → 케어 → 검증 → 보상)를 몸으로 익히게 해야 한다.
- 일반 모드는 실제 센서·실제 식물이 필요해서 시연 목적과 맞지 않고, 치트모드는 처음부터 모든 권한을 줘서 "게임을 배우는" 단계가 없다.
- **체험모드 = 둘 사이의 다리.** 맨몸(Lv.1, 무소유)으로 시작해 케어 액션만으로 빠르게 레벨업하고, 게이트 레벨에 도달하면 치트모드 전권이 열린다. 게임이 재미있으면 계속 키우고, 아니면 치트로 콘텐츠만 훑어보면 된다.
- 기존 치트 샌드박스(`public/farm/cheat.js`, `window.PMCheat`)와 동일하게 **localStorage 전용 — Supabase·하드웨어·실데이터는 절대 건드리지 않는다.**

## 2. 요구사항 ↔ 설계 매핑

| # | 요구사항 | 설계 결정 |
|---|---|---|
| 1 | Lv.1 · 아이템/수집요소 전무 상태로 시작 | 샌드박스를 **빈 시드**로 활성화 (level 1 · 0 XP · 0 Seeds · 0일). `pm_trial=1` 쿠키로 서버 렌더 페이지(컬렉션/상점/퀘스트)도 "아무것도 없음" 뷰 강제 |
| 2 | 일정 레벨 도달 시 치트모드 해제, My Garden 체류 2분 이내 | 게이트 = **Lv.6 (누적 75 XP)** — 꽃 스프라이트가 피는 레벨. 역산상 통상 80~105초 도달 (§4) |
| 3 | 케어 액션에 XP 부여 + 인게임 시간 경과 | 유효 액션 **+5 XP** / 비유효 +1 XP, 액션 **3회 = 1일** 경과 (§4.2, §4.3) |
| 4 | Happy 유지 시 실시간 소량 XP | **3초당 +1 XP** 드립 (분당 20 XP) |
| 5 | 주기적 돌발 이벤트로 mood 강제 전환 | 이벤트 스케줄러가 센서값을 직접 흔들어 mood를 바꿈. Happy 10~20초 지속 시 발동 (§5) |
| 6 | Seed 보상 방식 | 이벤트 해결 +10 · 하루 경과 +5 · 레벨업 +5 → 2분에 약 50~65 Seeds = 상점 최저가(20) **2~3개** 구매 가능 (§4.5) |
| + | 체험모드에서도 치트모드 강제 진입 가능 | 설정의 치트모드 버튼은 체험모드 중에도 **잠그지 않는다** — 누르면 샌드박스 상태를 유지한 채 즉시 치트모드로 전환 (수업 현장 비상구, §3) |
| + | 레벨당 XP 곡선 변경 가능 | **변경하지 않음.** 실게임과 동일한 15 XP/레벨(`src/types/game.ts`)이 이미 충분히 빠르고, 실게임과 같은 레벨 감각을 가르치는 게 온보딩 목적에 부합 |

## 3. 모드 3형제 — 위치 관계

```text
체험모드 (신규)             치트모드 (기존)              개발자모드 (기존)
─────────────             ─────────────              ──────────────
localStorage 샌드박스       localStorage 샌드박스        실제 Supabase row 수정
빈 상태에서 시작            현재 상태 복제로 시작          /settings?dev=1, 암호 "one"
케어 액션 = 게임플레이       케어 액션 + 수치 직접 편집     
XP/Seed/일수 자동 진행      수치를 손으로 조작            
Lv.6 도달                  처음부터 전권
→ 치트모드 해제 및 진입                      
```

- 세 모드는 **상호 배타**. 체험모드 진입 시 치트·개발자 모드를 닫는다.
- **강제 진입 비상구**: 학교 수업 시연은 온갖 변수가 생기므로, 체험모드에서 레벨을 못 올렸더라도 설정 페이지의 치트모드 활성화 버튼은 **항상 동작**한다. 누르면 현재 샌드박스 상태(레벨·XP·Seeds·센서값)를 유지한 채 `mode`만 `cheat`로 전환 — 게이트는 "자연스러운 해제 연출"일 뿐, 하드 락이 아니다. 설정 카드에는 게이트 미달 시 진행률("Lv.6까지 nn XP")을 함께 보여줘서 정상 경로도 안내한다.

## 4. 수치 설계와 역산

### 4.1 게이트 레벨 = Lv.6 (누적 75 XP)

근거:

- 레벨 공식은 `floor(totalXp / 15) + 1` — 레벨당 15 XP, 변경 없음.
- Lv.6은 두 번째 스프라이트 변화 지점(`level-bands.ts`: Lv.3 새싹 → **Lv.6 꽃**). **게이트 도달 순간 = 꽃이 피는 순간** — 치트 해제 축하와 성장 변화가 하나의 피크로 합쳐져 세션 전체의 클라이맥스가 된다. 서사도 깔끔하다: 새싹을 보고(Lv.3) → 꽃을 피우면(Lv.6) → 전권 해제.
- Lv.5(60 XP)는 이벤트 주기가 10~20초로 짧아진 뒤 65~90초에 열려 너무 이르고, 꽃 직전이라 해제 연출과 성장 연출이 따로 논다.
- Lv.7(90 XP)은 스프라이트 변화가 없는 밋밋한 레벨인 데다 느린 사용자가 2분을 넘길 위험이 있고, Lv.9(120 XP, 리본 꽃)는 통상 사용자도 2분을 초과한다.

### 4.2 케어 액션 XP: 유효 +5 / 비유효 +1, 쿨다운 1.5초

- **유효 액션** = 누른 순간 최소 1개 센서값이 권장 밴드 쪽으로 움직이는 액션 (예: Overheating 중 "그늘로 옮기기"). +5 XP — 3회면 1레벨이라 첫 레벨업이 20~30초 안에 터진다.
- **비유효 액션** = 이미 Happy인데 아무거나 누르는 등. +1 XP — 탐색 자체는 보상하되, 연타로 게이트를 뚫는 건 막는다.
- 같은 액션 1.5초 쿨다운(쿨다운 중 0 XP)으로 스팸 클릭 차단.
- 판정은 치트 샌드박스가 이미 갖고 있는 밴드(`setBands`로 주입된 작물 프로파일)와 `toggleTargets`/`applyDelta` 결과를 재활용하면 된다.

### 4.3 인게임 시간: 액션 3회 = 1일 (요구안 4회에서 하향 추천)

- 2분 세션에서 통상 6~9회 누르게 되는데, 4회/일이면 1~2일밖에 안 지나 날짜 변화가 체감되지 않는다. **3회/일이면 2~3일**이 지나 상단 일수 카운터와 주야 배경 전환이 눈에 들어온다.
- 날이 바뀔 때:
  - **잠카추 말풍선이 날짜 경과를 크게 알린다** — "Today is Day 3! 🌅" (ID: "Sekarang Hari ke-3! 🌅"). days 숫자만 조용히 올라가면 사용자가 시간 경과를 인지하지 못하므로, 말풍선을 일반 대사보다 눈에 띄는 스타일(배지 + 강조색)로 3~4초 고정 표시하고 배경 주/야 플래시(기존 painted day/night 배경 재활용)를 함께 터뜨린다.
  - 일수 +1, **+5 Seeds** (reason chip: "+5 🌰 · 새로운 하루!").
  - §5의 이벤트 발동 기회 1회.
- **예외 — 토양(pH) 액션은 하루가 아니라 며칠을 스킵한다** (§5.2): 재 뿌리기·부엽토 섞기·물로 헹구기·새 흙 섞기는 현실에서 며칠 걸리는 작업이므로, 1회 누르면 액션 카운터 대신 **즉시 +3일**이 경과한다 (날짜 말풍선은 "3일이 지났어… Today is Day 6!"처럼 스킵을 명시).

### 4.4 Happy 드립: 3초당 +1 XP

- 요구안 그대로. 분당 20 XP — 드립만으로는 게이트까지 3분이 걸리므로, 액션과 이벤트 해결이 주 수입원이 되는 균형이 유지된다.
- mood가 Happy가 아니면 드립 정지 → "Happy를 유지해야 이득"이라는 규칙이 수치로 전달된다.

### 4.5 Seeds: 이벤트 해결 +10 · 하루 경과 +5 · 레벨업 +5

- 목표: **2분에 상점 최저가(20 Seeds) 상품 2~3개** 구매 가능. 상점 탭이 "구경만 하는 곳"이 아니라 실제로 여러 번 소비하는 루프가 된다.
- 2분 플레이 기대치: 이벤트 해결 2~3회(20~30) + 2~3일 경과(10~15) + 레벨업 4회(20) ≈ **50~65 Seeds** → 20~25 Seeds대 아이템 2~3개.
- 토양 이벤트가 뜨면 해결 +10에 3일 스킵 보상(+15)까지 얹혀 **한 방 +25 Seeds** — 등장 빈도가 낮은 대신(§5.2) 잭팟처럼 느껴지는 구성.
- 실경제(퀘스트 +3, 스트릭 +1, `src/game/economy/seed-grants.ts`)보다 크게 부풀린 값이지만, 샌드박스 전용이라 실경제 밸런스에는 영향 없음.

### 4.6 타임라인 역산 (통상 사용자 시뮬레이션)

| 구간 | 벌어지는 일 | XP 수입 | 누적 XP |
|---|---|---|---|
| 0–15s | 등장 + 말풍선 안내, Happy 드립 | 드립 +5 | 5 |
| 15s | 🔥 이벤트 1 발동 (Overheating) | — | 5 |
| 15–28s | 그늘/부채질 액션 2회로 해결 | 액션 +10 · 해결 +10 | 25 |
| 28–42s | Happy 드립 + 호기심 액션 1회, 1일차 경과 ("Today is Day 1!") | 드립 +4 · 액션 +5 | 34 |
| ~42s | 💨 이벤트 2 발동 (해결 후 10~20초 경과) | — | 34 |
| 42–55s | 분무 2회로 해결 | 액션 +10 · 해결 +10 | 54 |
| 55–70s | Happy 드립, 2일차 경과 ("Today is Day 2!") | 드립 +5 | 59 |
| ~70s | 🌫️ 이벤트 3 발동 (해결 후 10~20초 경과) | — | 59 |
| 70–85s | 램프 켜기 등 액션 2회로 해결 | 액션 +10 · 해결 +10 | **79 → 75 XP 돌파 = Lv.6, 꽃 개화 + 게이트 해제 (~80–90초)** |

- 통상 사용자는 **80~105초**에 게이트 도달 — 2분 요구 안에 들어오면서, 해제 직전 이벤트를 하나 더 풀게 되어 루프 학습량도 늘어난다.
- 느린 사용자(이벤트 절반만 스스로 해결, 액션 절반이 비유효)는 110~120초 언저리. 이벤트 힌트(§5.3)가 있어 완전히 막히는 경우는 없고, 최악의 경우에도 설정의 치트모드 강제 진입 비상구(§3)가 있다.
- 잘하는 사용자는 ~75초 도달 — 지루해지기 전에 게이트가 열린다.

## 5. 돌발 이벤트 시스템

### 5.1 발동 규칙

- 체험모드 시작 **15초 후** 첫 이벤트 발동 (온보딩 여유).
- 이후 다음 이벤트는 **직전 이벤트가 해결된 시점부터 10~20초(랜덤)** 뒤에 발동한다. 타이머의 기산점은 반드시 **해결(Happy 복귀) 순간** — 발동 시점이 아니다. Happy가 아닌 동안은 타이머가 돌지 않으므로 문제가 겹쳐 쌓이지 않는다.
- 하루가 바뀌는 순간에도 발동 기회 1회 ("새 날이 밝았는데… 앗!"). 단, 날짜 말풍선(§4.3)이 먼저 3~4초 표시된 뒤 발동해 두 연출이 겹치지 않게 한다.
- 발동 시 **잡고 있던 토글을 전부 해제**한다 (예: "바람에 덮개가 날아갔어!"). 사용자가 "그늘" 토글을 켜둔 채라면 폭염 이벤트가 시뮬레이션 틱에 의해 저절로 풀려버리기 때문 — 반드시 사용자의 새 행동을 요구하게 만든다.

### 5.2 이벤트 풀 (센서값 변경 → mood 강제 전환)

센서값은 `PMCheat.set({ vitals })`로 직접 쓰고, mood는 기존 `cheatMoodFor`가 파생한다. 목표값은 하드코딩이 아니라 **활성 작물 프로파일의 밴드 기준**(치트 샌드박스의 `bands`와 동일한 방식)으로 계산한다.

| 이벤트 | 연출 문구(예) | 센서 조작 | 결과 mood | 정답 액션 예 |
|---|---|---|---|---|
| 🔥 뜨거운 햇살 | "햇볕이 너무 뜨거워!" | temp → overheatEnter+5, light → 90 | Overheating | 그늘로 이동, 부채질 |
| 🌫️ 흐린 날 | "구름이 해를 가렸어…" | light → 10 | Sleepy | 램프 켜기, 해로 이동 |
| 💨 건조한 바람 | "바람이 너무 건조해!" | humidity → dryEnter−10 | DryAir | 분무, 비닐 덮기 |
| 🌧️ 눅눅한 소나기 | "공기가 너무 축축해!" | humidity → humidAbove+15 | HumidAir | 창문 열기, 부채질 |
| 🥶 추운 밤 | "밤공기가 차가워!" | temp → coldAt−3 | TooCold | 따뜻한 곳으로, 손으로 감싸기 |
| 🧪 산성 토양 | "흙이 시큼해졌어…" | soilPh → recMin−0.8 | SoilAcidic | 재 뿌리기, 새 흙 섞기 |
| 🧪 알칼리 토양 | "흙이 이상해…" | soilPh → recMax+0.8 | SoilAlkaline | 부엽토 섞기, 물로 헹구기 |

- 직전 이벤트와 같은 종류는 연속 발동 금지(마지막 2개 제외 후 랜덤). 토양 이벤트는 풀에서 가중치를 낮게.
- **토양 이벤트의 특수 규칙 — 며칠 스킵**: pH를 바꾸는 액션(재 뿌리기·부엽토 섞기·물로 헹구기·새 흙 섞기)은 현실에서 며칠 걸리는 작업이다. 체험모드에서는 이 액션 1회가 pH를 즉시 바꾸는 대신 **인게임 시간 +3일을 스킵**한다 (액션 카운터 미적용, 날짜 말풍선이 "3일이 지났어… Today is Day 6!"로 스킵을 명시, 스킵된 날에도 하루 보상 +5 Seeds×3 적용). 케어 액션 버튼의 기존 `⏳ days later` 배지가 이 규칙의 예고편 역할을 한다 — "느린 작업 = 시간이 통째로 지나간다"는 현실 감각을 게임 문법으로 번역한 것.
- **해결 판정**: mood가 다시 Happy로 복귀하는 순간. **+10 XP, +10 Seeds**, 축하 연출(기존 celebration queue의 reason chip 재활용: "+10 XP · 위기 해결!").

### 5.3 막힌 사용자 구제

이벤트 발동 후 **20초간 mood가 복귀하지 않으면** 잠카추 말풍선이 정답 액션을 직접 가리킨다 ("그늘이 필요해… 🌳 버튼을 눌러줘!"). 시연 현장에서 아무도 방치되지 않게 하는 안전망.

## 6. UX 플로우

```text
/settings의 "체험 모드" 카드 (치트모드 카드 위에 배치)
        ↓ [🎮 체험 모드 시작]
빈 시드로 샌드박스 활성화 + pm_trial 쿠키 + My Garden으로 전체 리로드
        ↓
상단 배너: "🎮 체험 모드 — 실제 데이터는 안전해요 · Lv.6이 되면 치트모드가 열려요! (진행률 바)"
        ↓
케어 액션 패널 (치트모드의 CARE ACTIONS 섹션 재사용, 수치 직접 편집 UI는 숨김)
드립 XP → 첫 레벨업 → 이벤트 → 해결 → Seeds → 반복
        ↓ Lv.6 도달
게이트 축하 연출 — 꽃 스프라이트 개화 + 레벨업 FX + "치트모드가 열렸어요!" 카드가
하나의 시퀀스로 재생 (세션의 클라이맥스, §4.1)
        ↓ 사용자가 선택
[계속 키우기]  또는  [🎛️ 치트모드로 전환]
                        ↓
             샌드박스 상태 유지한 채 mode만 cheat로 전환
             (DEMO CONTROLS 패널 등장, 수치 직접 편집 + ownAll/revealAll 가능)
```

- 배너의 진행률 바가 **항상 게이트까지 남은 XP를 보여준다** — 목표가 화면에 상주해야 2분짜리 동기가 성립한다.
- **비상구(§3)**: 게이트 도달 여부와 무관하게, 설정 페이지의 치트모드 활성화 버튼을 누르면 언제든 샌드박스 상태를 유지한 채 치트모드로 전환된다. 수업 시연 중 시간이 밀리거나 사용자가 막혔을 때를 위한 안전판.
- 종료(배너 Exit)는 치트모드와 동일: localStorage 삭제 + 쿠키 해제 + 리로드 → 무결한 일반 모드 복귀.
- 게이트 도달 이후에도 체험모드를 유지하면 모든 시스템 그대로 계속 (Lv.30 캡까지).

## 7. "무소유 시작"의 범위 (요구사항 1)

| 영역 | 체험모드에서의 상태 | 구현 방법 |
|---|---|---|
| 레벨/XP/일수/Seeds | 1 / 0 / 0 / 0 | 빈 시드로 `activate()` |
| 상점 아이템 | 전부 미보유 | `shop.ownAll=false` + `pm_trial` 쿠키 → 서버 렌더가 보유목록을 비워서 내려줌 |
| 컬렉션 (mood/배지/스토리/지혜) | 전부 잠김 (실루엣) | `collection.revealAll=false` + `pm_trial` 쿠키 → 실계정 보유분 숨김 |
| 스킨/장식 | Lv.1 기본 잠카추만 | 레벨 파생이므로 자동 (level-bands가 알아서) |
| 케어 스트릭 | 0에서 시작, 인게임 일수로 진행 | 샌드박스 `status.days` |

주의: 치트모드의 쿠키(`pm_cheat`)는 "더 많이 보여주기"용인데, 체험모드 쿠키(`pm_trial`)는 반대로 **"실계정 보유분 숨기기"**용이다. 서버 컴포넌트에서 이 쿠키를 읽어 보유/해금 데이터를 빈 값으로 치환하는 분기가 각 페이지(collection·shop·quests)에 필요하다.

## 8. 가드레일

1. **실데이터 격리** — 치트 샌드박스와 동일 원칙: localStorage 밖으로는 아무것도 쓰지 않는다. Supabase·게임 API·Node-RED 호출 전면 차단 (기존 `main()`의 sandbox 분기 재사용).
2. **윤리 가드레일과의 관계** — README의 "물리 케어 XP는 센서 검증 퀘스트에서만"은 **실제 진행도**에 대한 원칙이다. 체험모드 XP는 배너로 명시된 샌드박스 안의 가짜 진행도이며 절대 영속화되지 않으므로 원칙과 충돌하지 않는다. 단, 이 구분이 흐려지지 않도록 배너 문구에 "연습용 정원 — 진짜 기록에 남지 않아요"를 명시한다.
3. **접근성** — 모든 연출은 `prefers-reduced-motion` 존중(기존 celebration queue 규약), 신규 문구는 전부 ID/EN 이중언어(`strings.js` 및 `CHEAT_LABELS` 패턴).
4. **모드 배타성** — 진입 시 `sessionStorage`의 dev 코드 제거(치트모드 진입과 동일), 치트모드가 이미 활성인데 체험모드를 켜면 확인 후 치트 상태 폐기.
5. **탭 절전/새로고침** — 이벤트·드립 타이머는 치트 틱과 같은 방식으로 경과 실시간 기준(`MAX_STEP_MS` 캡) 계산. 새로고침해도 localStorage에서 그대로 복원.

## 9. 구현 항목 (파일별)

| 파일 | 작업 |
|---|---|
| `public/farm/cheat.js` | 상태에 `mode: "cheat" \| "trial"` 필드 추가, 배너 문구 모드 분기(+진행률 바), `pm_trial` 쿠키 셋/클리어, `activate(seed, mode)` 시그니처 확장 |
| `public/farm/trial.js` **(신규)** | 체험모드 게임 엔진: Happy 드립 티커, 이벤트 스케줄러(해결 시점 기산 10~20초) + 이벤트 풀, 액션 XP 훅(`press` 래핑: 유효 판정·쿨다운), 3액션=1일 카운터 + 토양 액션 3일 스킵, 날짜 경과 알림 큐, Seed 지급(해결 +10 · 하루 +5 · 레벨업 +5), 게이트 감지 → `pmtrial:gate` 이벤트. 모든 상태 변경은 `PMCheat.set()` 경유 |
| `src/app/layout.tsx` | `<script src="/farm/trial.js">` 추가 (cheat.js 뒤) |
| `public/farm/live.js` | trial 모드에서: DEMO CONTROLS의 수치 편집 숨기고 케어 액션만 노출, 이벤트 연출(말풍선+토스트), 날짜 경과 말풍선("Today is Day 3! 🌅" 강조 스타일 3~4초 고정 + 주/야 플래시), 해결/게이트 축하 연출, 드립 XP의 reason chip |
| `src/lib/pm-cheat.ts` | 타입에 `mode` 추가, `useTrial()` 훅 (or `useCheat` 확장) |
| `src/components/trial-mode-toggle.tsx` **(신규)** | 설정 페이지 진입 카드 (cheat-mode-toggle.tsx 패턴 복제, 빈 시드로 활성화) |
| `src/components/cheat-mode-toggle.tsx` | 체험모드 활성 시: 활성화 버튼은 **그대로 동작하되** 샌드박스 상태를 유지한 채 `mode`를 `cheat`로 전환(비상구), 게이트 미달이면 버튼 옆에 진행률 안내("Lv.6까지 nn XP — 정원에서 키워도 열려요") 표시 |
| `src/app/settings/page.tsx` | 체험모드 카드 배치 |
| `src/app/collection/page.tsx` · `shop/` · `quests/` | `pm_trial` 쿠키 분기: 보유/해금 데이터를 빈 값으로 치환 |
| `src/game/dev/trial-constants.ts` **(신규, 선택)** | 수치 상수(게이트 레벨·XP량·주기)를 TS 한 곳에 정의하고 테스트가 trial.js와 핀 고정 (기존 `SENSOR_LIMITS` 미러링 패턴) |
| `tests/trial-mode.test.ts` **(신규)** | §10 테스트 |
| `README.md` / `docs/` | 모드 3형제 문서화. (부수 수정: README의 "레벨당 30 XP" 서술이 코드의 15 XP와 불일치 — 이번에 바로잡기) |

## 10. 테스트 계획 (Vitest)

- 빈 시드 활성화 → level 1 / 0 XP / 0 Seeds / 0일 / ownAll·revealAll false.
- 유효 액션 +5 / 비유효 +1 / 쿨다운 중 0 XP.
- 액션 3회 → days +1, +5 Seeds, 날짜 알림이 큐에 쌓임.
- 토양(pH) 액션 1회 → days +3, 하루 보상 +5 Seeds×3, 액션 카운터는 증가하지 않음.
- Happy에서 드립 티커가 3초당 +1, 비Happy에서 정지 (fake timers).
- 이벤트 발동 시: 센서값이 밴드 기준으로 이동해 의도한 mood가 파생되는지 (7종 전부), 토글 전원 해제되는지, 동일 이벤트 연속 발동 금지.
- 이벤트 재발동 타이머: 기산점이 **직전 이벤트 해결 시점**이고 10~20초 범위인지, Happy가 아닌 동안 멈추는지 (fake timers).
- 해결 판정: Happy 복귀 시 +10 XP·+10 Seeds가 정확히 1회.
- 게이트: 75 XP 도달 시 `pmtrial:gate` 발화, 치트 전환 시 mode만 바뀌고 상태 보존.
- 비상구: 게이트 미달 상태에서 치트모드 활성화 → mode가 `cheat`로 바뀌고 level/XP/Seeds/vitals 보존.
- 수치 상수 핀: trial.js ↔ trial-constants.ts 일치.
- 격리: 체험모드 동안 fetch/Supabase 호출이 0건 (기존 cheat-sandbox-wiring 테스트 패턴).

## 11. 구현 결과 — 계획과 달라진 점

구현·브라우저 검증 과정에서 실제 화면이 알려준 것들. 모두 기획 의도는 유지하되 방법이 바뀐 항목이다.

| 항목 | 계획 | 실제 구현 | 이유 |
|---|---|---|---|
| 케어 패널 위치 | 치트 패널의 기존 도크 재사용 | **잠카추 왼쪽 빈 스트립**에 세로 배치 (폭 190px) | 기존 도크는 말풍선을 74px 가리고 캐릭터도 덮었다. 발표자는 패널을 드래그해 치우지만 학생은 그러지 않는다. 스테이지(724px) 안에서 말풍선(최대 520px)과 패널은 가로로 공존할 수 없어, 캐릭터 컬럼 바깥이 유일한 답 |
| 날짜 말풍선 | 강조 스타일로 3~4초 고정 | 고정 + **mood 구름을 그동안 숨김** | 구름이 말풍선 오른쪽에 떠 있어 "3 days passed… Today is Day 6!"의 뒷부분을 먹었다. 구름의 `pm-chip-float` 애니메이션이 opacity를 덮어써서 `animation: none !important`가 필요 |
| 알림 종료 처리 | `applyCheatFarm`이 복구 | **직전 대사를 저장했다가 복원** | 밤에는 `sleepShown`이 말풍선을 소유해 `applyCheatFarm`이 복구를 건너뛰고, 알림이 영구히 남아 구름에 잘렸다 |
| 막힌 사용자 힌트 | 정답 액션들을 나열 | **단 하나의 버튼만** 지목 | 말풍선이 좁고, 막힌 학생에게 필요한 건 선택지가 아니라 다음 한 걸음 |
| 체험모드 상점 | 서버가 빈 목록만 내려줌 | 그에 더해 **잔액을 샌드박스에 연결하고 구매 시 실제 차감** | Seed를 벌게 해놓고 쓸 수 없으면 §4.5의 "2~3개 구매" 목표가 성립하지 않는다 |
| `<body data-cheat>` 마커 | (기존 동작 유지) | **제거** | `beforeInteractive` 스크립트가 React 하이드레이션 전에 body 속성을 써서 모든 React 라우트에서 hydration mismatch가 났다. `setTimeout(0)` 지연으로도 경합에서 졌고, 앱 어디서도 읽지 않는 값이었다 |
| 슬로우 액션 안내문 | 패널에 유지 | 체험모드 패널에서 **제거** | 버튼의 `⏳ days later` 배지가 같은 말을 하고, 실제로 3일이 건너뛰는 연출이 문장보다 강하게 가르친다. 줄어든 높이가 곧 말풍선을 피할 여유 |

검증 상태: Vitest 1,129개 전부 통과(체험모드 44개 신규), lint 0 errors, 프로덕션 빌드 성공, 브라우저에서 전 구간 실플레이 확인 — 빈 상태 시작 → 케어 XP → 돌발 이벤트 → 힌트 → 해결 → 날짜 경과 → 토양 3일 스킵 → Lv.6 게이트 카드 → 비상구 전환(진행도 보존) → 상점 구매 차감 → Exit 복귀.

## 12. ⚠️ 병합으로 무효화된 전제 — 게이트 레벨 재검토 필요

체험모드 작업 중 `origin/main`에 스프라이트 레벨 밴드 전면 재설계가 들어왔다(`level-bands.ts`, 7밴드 → **15밴드**).

| | 이전 (Lv.6 선택 당시) | 현재 (병합 후) |
|---|---|---|
| 밴드 경계 | 1, 3, 6, 9, 13, 18, 24 | 1, 3, 5, 7, 9, 11, 13, 15 … (홀수마다) |
| Lv.6 | **꽃 개화** (phase 3) | 밴드 3 중간 — **화면상 아무 변화 없음** |
| 꽃 | Lv.6 | Lv.15 "Strawberry Bloom" = 210 XP (2분 내 도달 불가) |

**Lv.6을 고른 유일한 근거였던 "게이트 도달 = 꽃 피는 순간"이 사라졌다.** 게이트 자체는 정상 동작하고(75 XP · 약 80~105초) 시간 예산도 그대로지만, 이제 아무 시각적 변화와도 겹치지 않는다.

**추천: Lv.5 (60 XP)로 이동.** 새 밴드에서 Lv.5는 밴드 3 "Tiny Sprout"의 시작이자 **씨앗 → 새싹 phase 전환** — 새 사다리에서 2분 안에 닿을 수 있는 유일한 진짜 형태 변화다. 측정상 65~90초로 시간 여유도 더 크다. (Lv.7은 같은 phase의 크기만 커지는 단계라 의미가 약하고 100초를 넘긴다.)

변경 시 손댈 곳은 `TRIAL_GATE_LEVEL` 한 곳뿐이다(`cheat.js`의 미러 상수와 테스트 핀 포함 — 테스트가 불일치를 잡아준다). 현재는 사용자가 명시적으로 고른 값이므로 **Lv.6을 유지한 채** 이 항목을 남겨둔다.

## 13. 확정 필요한 사항 (기본값으로 진행 가능)

1. **게이트 도달 시 자동 전환 vs 선택** — 기획안은 "선택"(계속 키우기 / 치트 전환 버튼). 시연 진행상 자동 전환이 낫다면 변경 용이.
2. **하루 = 3액션** — 요구안은 4회였으나 3회 추천(§4.3). 4회 유지도 무방.
3. 체험모드 중 **퀴즈/농부 챗 등 부가 콘텐츠** 노출 여부 — 기본은 "보이되 샌드박스라 기록 안 됨" 안내. 완전 숨김도 가능.
4. **토양 액션의 스킵 일수** — 기본 +3일. 스킵 보상(+15 Seeds)이 과하다고 판단되면 스킵된 날의 하루 보상을 절반(+2~3 Seeds/일)으로 줄이는 선택지도 있음.
