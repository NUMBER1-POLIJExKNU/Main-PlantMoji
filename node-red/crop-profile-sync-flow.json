[
  {"id":"crop-profile-sync-tab","type":"tab","label":"Crop Profile Sync","disabled":false,"info":"Fetch selected profile at startup and every 60 seconds."},
  {"id":"crop-profile-poll","type":"inject","z":"crop-profile-sync-tab","name":"Startup + every 60s","props":[{"p":"payload"}],"repeat":"60","once":true,"onceDelay":"0.5","topic":"","payload":"","payloadType":"date","x":150,"y":100,"wires":[["crop-profile-request"]]},
  {"id":"crop-profile-request","type":"function","z":"crop-profile-sync-tab","name":"Prepare profile request","func":"const baseUrl = env.get('PLANTMOJI_API_URL') || 'http://127.0.0.1:3000';\nconst token = env.get('DEVICE_API_TOKEN');\nmsg.method = 'GET';\nmsg.url = `${baseUrl.replace(/\\/$/, '')}/api/crop-profile?plantId=plant-01`;\nmsg.headers = token ? {Authorization:`Bearer ${token}`} : {};\nreturn msg;","outputs":1,"noerr":0,"initialize":"","finalize":"","libs":[],"x":390,"y":100,"wires":[["crop-profile-http"]]},
  {"id":"crop-profile-http","type":"http request","z":"crop-profile-sync-tab","name":"GET crop profile","method":"use","ret":"obj","paytoqs":"ignore","url":"","persist":false,"authType":"","senderr":true,"headers":[],"x":620,"y":100,"wires":[["crop-profile-cache"]]},
  {"id":"crop-profile-cache","type":"function","z":"crop-profile-sync-tab","name":"Validate + cache last success","func":"const body = msg.payload;\nconst p = body && body.ok === true && body.profile;\nconst valid = p && p.key === 'strawberry' && Number.isFinite(Number(p.version)) && p.temperature && p.airHumidity && p.soilPh && p.light;\nif (!valid) { const cached = flow.get('cropProfile'); node.status({fill:cached?'yellow':'red',shape:'ring',text:cached?'API failed · keeping v'+cached.version:'API failed · cold start fallback'}); return null; }\nconst next = {key:p.key,version:Number(p.version),plantName:p.plantName,timezone:p.timezone,temperatureHighOn:Number(p.temperature.overheating.enterAtOrAbove),temperatureHighOff:Number(p.temperature.overheating.recoverAtOrBelow),humidityLowOn:Number(p.airHumidity.dryAir.enterBelow),humidityLowOff:Number(p.airHumidity.dryAir.recoverAtOrAbove),soilPHMin:Number(p.soilPh.recommended.min),soilPHMax:Number(p.soilPh.recommended.max),soilPHHysteresis:0,lightMinimumPercent:Number(p.light.minimumPercentDuringLightingHours),dayStartHour:Number(p.light.lightingHours.start),dayEndHour:Number(p.light.lightingHours.end),timezoneOffsetHours:7};\nflow.set('cropProfile', next); node.status({fill:'green',shape:'dot',text:`${next.key} v${next.version}`}); msg.payload = next; return msg;","outputs":1,"noerr":0,"initialize":"","finalize":"","libs":[],"x":880,"y":100,"wires":[[]]},
  {"id":"combine-profile-replacement","type":"function","z":"crop-profile-sync-tab","name":"REPLACE: Combine Plant State","func":"const fallback = {key:'strawberry',version:1,plantName:'Strawberry',temperatureHighOn:28,temperatureHighOff:26,humidityLowOn:40,humidityLowOff:45,soilPHMin:5.5,soilPHMax:6.5,soilPHHysteresis:0,lightMinimumPercent:30,timezoneOffsetHours:7,dayStartHour:6,dayEndHour:18};\nconst c = flow.get('cropProfile') || fallback; const d = msg.payload; const now = Number(d.timestamp) || Date.now();\nlet isDaytime = d.isDaytime; if (typeof isDaytime !== 'boolean') { const h = new Date(now + c.timezoneOffsetHours*3600000).getUTCHours(); isDaytime = c.dayStartHour < c.dayEndHour ? h >= c.dayStartHour && h < c.dayEndHour : h >= c.dayStartHour || h < c.dayEndHour; }\nlet control = flow.get('environmentControl') || {ventOpen:false,airDry:false}; if (!control.ventOpen && d.temperature >= c.temperatureHighOn) control.ventOpen = true; else if (control.ventOpen && d.temperature <= c.temperatureHighOff) control.ventOpen = false; if (!control.airDry && d.humidity < c.humidityLowOn) control.airDry = true; else if (control.airDry && d.humidity >= c.humidityLowOff) control.airDry = false;\nlet soil = flow.get('soilPHStatus') || 'Optimal'; if (d.soilPH < c.soilPHMin) soil = 'Acidic'; else if (d.soilPH > c.soilPHMax) soil = 'Alkaline'; else soil = 'Optimal'; flow.set('environmentControl',control); flow.set('soilPHStatus',soil); msg.profile=c; msg.analysis={isDaytime,overheating:control.ventOpen,ventOpen:control.ventOpen,airDry:control.airDry,lowLight:isDaytime && d.light < c.lightMinimumPercent,soilStatus:soil}; return msg;","outputs":1,"noerr":0,"initialize":"","finalize":"","libs":[],"x":420,"y":220,"wires":[[]]},
  {"id":"crop-profile-note","type":"comment","z":"crop-profile-sync-tab","name":"Copy replacement into v5; see node-red/README.md","info":"Disconnected so import cannot alter a running graph.","x":440,"y":280,"wires":[]}
]
